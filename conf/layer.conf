# We have a conf and classes directory, append to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have a recipes directory, add to BBFILES
BBFILES += "${LAYERDIR}/recipes*/*/*.bb ${LAYERDIR}/recipes*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-edgeai"
BBFILE_PATTERN_meta-edgeai := "^${LAYERDIR}/"

# We keep the priority higher than all others
BBFILE_PRIORITY_meta-edgeai = "13"

LAYERDEPENDS_meta-edgeai = " \
    core \
    meta-ti-bsp \
    meta-ti-extras \
    meta-ti-test \
    meta-arago-distro \
    meta-arago-extras \
    meta-ti-foundational \
"

LAYERSERIES_COMPAT_meta-edgeai = "whinlatter wrynose"

LAYERRECOMMENDS_meta-edgeai = " \
    qt6-layer \
    selinux \
"

BBFILES_DYNAMIC += " \
    selinux:${LAYERDIR}/dynamic-layers/selinux/recipes*/*/*.bbappend \
"

# Disable GBM in gstreamer
PACKAGECONFIG:remove:pn-gstreamer1.0-plugins-base = " gbm"


require conf/distro/include/branding-core.inc
require conf/distro/include/edgeai-source-ipk.inc
